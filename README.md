# p6
mkdir
cd
git init
git clone "repo link"
ls
cd repo name
git pull
git pull origin
git branch
(main)
git branch master
git branch feature
git checkout feature
git merge feature -m "Merged"
git remote set-url origin "https://token@repolink"
git push origin branch name (main)
